<!DOCTYPE html>
<html>
    <head>
        <title>ALZalz | account</title>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/baseCss.css">
    </head>
    <body>
        <%- include('blockHeader.ejs') %>

        <div id="base-main">
            <div class="side-bar">
            </div>
            <div class="main">
                <div class="jumbo"><h2>Profile</h2></div>
                <div class="container">
                    <h1><%= JSON.stringify(userData.username) %></h1>

                    <h2 style="margin: 0px;">푼 문제</h2>
                    <%= userData.clear %>
                    <h2 style="margin: 0px;">못 푼 문제</h2>
                    <%= userData.wrong %>
                    <form class="form-box" action="/logOut" method="POST" style="margin-top: 4%;">
                        <button type="submit">로그아웃</button>
                    </form> 
                </div>
            </div>
        </div>

        <%- include('blockFooter.ejs') %>
    </body>
</html>