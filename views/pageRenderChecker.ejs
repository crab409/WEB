<!DOCTYPE html>
<html>
    <head>
        <title></title>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/baseCss.css">
        <link rel="stylesheet" href="/css/problemSolve.css">
    </head>
    <body>
        <style>
            .container {
                padding: 4%;
            }

            .content-header h3{
                color: black;
                display: flex;
                justify-content: space-between;
            }
        </style>
        <%- include('blockHeader.ejs') %>


        <div id="base-main">
            <div class="side-bar">
                this is side bar
            </div>
            <div class="main">
                <div class="jumbo"><h2>문제</h2></div>
                <div class="container">
                    <div class="content-header"><h3>
                        <p><%= JSON.stringify(dataSet.title) %></p>
                    </h3></div>
                    <div class="content">
                        <%- dataSet.content.replace(/\n/g, '<br>') %>
                    </div>
                    <div class="explain">
                        <h4>입력 설명</h4>
                        <%= JSON.stringify(dataSet.inputExplain) %>
                        <h4>출력 설명</h4>
                        <%= JSON.stringify(dataSet.outputExplain) %>
                    </div>
                    <div class="sample">
                        <h4>입력 예제</h4>
                        <%= JSON.stringify(dataSet.sampleInput) %>
                        <h4>출력 예제</h4>
                        <%= JSON.stringify(dataSet.sampleOutput) %>
                    </div>
                    <h3>생성여부</h3>
                    <form class="form-box" action="/problemRenderSumit" method="POST">
                        <input name="title" value="<%= dataSet.title %>">
                        <input name="content" value="<%= dataSet.content %>">
                        <input name="inputExplain" value="<%= dataSet.inputExplain %>">
                        <input name="outputExplain" value="<%= dataSet.outputExplain %>">
                        <input name="sampleInput" value="<%= dataSet.sampleInput %>">
                        <input name="sampleOutput" value="<%= dataSet.sampleOutput %>">
                        <input name="testCase" value="<%= dataSet.testCase%>">
                        <input name="answerTable" value="<%= dataSet.answerTable %>">
                        <input name="createrId" value="<%= userData._id %>">
                        <button type="submit">생성</button>
                    </form>
                    <form class="cancel" action="/cancel" method="POST">
                        <button type="submit">취소</button>
                    </form>
                </div>
            </div>
        </div>


        <%- include('blockFooter.ejs') %>
    </body>
</html>