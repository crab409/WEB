<!DOCTYPE html>
<html>
    <head>
        <title>ALZalz | check</title>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/baseCss.css">
        <link rel="stylesheet" href="/css/problemSolve.css">
    </head>
    <body>
        <style>
            .container {
                padding: 4%;
            }

            .content-header h3{
                color: black;
                display: flex;
                justify-content: space-between;
            }
        </style>
        <%- include('blockHeader.ejs') %>


        <div id="base-main">
            <div class="side-bar">
                this is side bar
            </div>
            <div class="main">
                <div class="jumbo"><h2>문제</h2></div>
                <div class="container">
                    <div class="content-header"><h3>
                        <p><%= dataSet.title %></p>
                    </h3></div>
                    <div class="content">
                        <%- dataSet.content.replace(/\n/g, '<br>') %>
                    </div>
                    <div class="explain">
                        <div>
                            <h4>입력 설명</h4>
                            <%= dataSet.inputExplain %>
                        </div>
                        <div>
                            <h4>출력 설명</h4>
                            <%= dataSet.outputExplain%>
                        </div>
                    </div>
                    <div class="sample">
                        <div>
                            <h4>입력 예제</h4>
                            <%= dataSet.sampleInput %>
                        </div>
                        <div>
                            <h4>출력 예제</h4>
                            <%= dataSet.sampleOutput %>
                        </div>
                    </div>
                    <h3>생성여부</h3>
                    <form class="form-box" action="/problemRenderSumit" method="POST">
                        <input name="title" value="<%= dataSet.title %>" style="display: none;">
                        <input name="content" value="<%= dataSet.content %>"style="display: none;">
                        <input name="inputExplain" value="<%= dataSet.inputExplain %>"style="display: none;">
                        <input name="outputExplain" value="<%= dataSet.outputExplain %>"style="display: none;">
                        <input name="sampleInput" value="<%= dataSet.sampleInput %>"style="display: none;">
                        <input name="sampleOutput" value="<%= dataSet.sampleOutput %>"style="display: none;">
                        <input name="testCase" value="<%= dataSet.testCase%>"style="display: none;">
                        <input name="answerTable" value="<%= dataSet.answerTable %>"style="display: none;">
                        <input name="createrId" value="<%= userData._id %>"style="display: none;">
                        <button type="submit">생성</button>
                    </form>
                    <form class="cancel" action="/cancel" method="POST">
                        <button type="submit">취소</button>
                    </form>
                </div>
            </div>
        </div>


        <%- include('blockFooter.ejs') %>
    </body>
</html>